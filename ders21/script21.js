var text = "Hello world"; // index yani sayma sayısı 0 dan baslar (Javascript dilinde)

var new_text = text.substring(0, 5); //5 e kadar =>0 1 2 3 4  substring son değeri dahil etmez

document.write(new_text);